# Django settings
DJANGO_SECRET_KEY=replace-this-with-a-development-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Database settings
# Use PostgreSQL for shared/server development when database credentials are available.
# Vercel/managed PostgreSQL may instead provide DATABASE_URL; it takes precedence.
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
DB_ENGINE=django.db.backends.postgresql
DB_NAME=emr_claim_entry_portal
DB_USER=replace-with-database-user
DB_PASSWORD=replace-with-database-password
DB_HOST=127.0.0.1
DB_PORT=5432

# Development safety note
# Do not store real PHI in local development or sample data.
